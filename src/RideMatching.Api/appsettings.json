{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//SqlServer": "No default connection string with credentials is committed. Provide it via appsettings.Development.json (git-ignored), the ConnectionStrings__SqlServer environment variable, or a secret store. For production use Encrypt=True;TrustServerCertificate=False."
  },
  "Redis": {
    "ConnectionString": "localhost:6379"
  },
  "Jwt": {
    "Issuer": "ride-matching",
    "Audience": "ride-matching",
    "AccessTokenMinutes": 60,
    "//SigningKey": "No signing key is committed. Provide it via appsettings.Development.json (git-ignored), the Jwt__SigningKey environment variable, or a secret store."
  },
  "Matching": {
    "SearchRadiusKm": 5,
    "MaxCandidates": 10,
    "PresenceTtlSeconds": 30,
    "DriverFreshnessSeconds": 30
  }
}
